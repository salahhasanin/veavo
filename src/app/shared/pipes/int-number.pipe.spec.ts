import { IntNumberPipe } from './int-number.pipe';

describe('IntNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new IntNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
